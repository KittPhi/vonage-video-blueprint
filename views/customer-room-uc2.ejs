<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Customer Room</title>
    <link
      rel="stylesheet"
      href="<%= appURI.replace(/\/+$/, '') %>/css/style.css"
    />
    <%- include('common/styles'); -%>
    <link
      rel="stylesheet"
      href="<%= appURI.replace(/\/+$/, '') %>/css/customer-room.css"
    />

    <script src="https://video.standard.vonage.com/v2/js/opentok.min.js"></script>
    <script type="text/javascript">
      var apiKey = "<%= vonageData.apiKey %>";
      var sessionId = "<%= vonageData.sessionId %>";
      var token = "<%= vonageData.token %>";
      var userName = "<%= vonageData.userName %>";
      var roomRole = "<%= roomRole %>";
      var publishAudio = <%- JSON.stringify(publishAudio) %>;
      var useCase = <%- JSON.stringify(useCase) %>;
    </script>
    <style>
      .agent-speaking-indicator {
        position: absolute;
        right: 1rem;
        bottom: 1rem;
        z-index: 10;
        width: 190px;
        background: rgba(0, 0, 0, 0.35);
        border: 2px solid rgba(255, 255, 255, 0.25);
        border-radius: 10px;
        padding: 0.55rem;
        transition: border-color 120ms ease-in-out;
      }

      .customer-speaking-indicator {
        border: 2px solid rgba(255, 255, 255, 0.22);
        border-radius: 10px;
        transition: border-color 120ms ease-in-out;
      }

      .agent-speaking-indicator.is-speaking {
        border-color: #2bd576;
      }

      .customer-speaking-indicator.is-speaking {
        border-color: #2bd576;
      }

      .agent-audio-placeholder {
        width: 100%;
        height: 68px;
      }

      .customer-main-tile {
        position: relative;
      }

      .customer-main-tile #publisher {
        width: 100%;
      }
    </style>
  </head>
  <body>
    <%- include('common/header'); -%>

    <div class="container customer-room">
      <div class="container">
        <div class="row">
          <!-- Customer's own video (published) -->
          <div class="col-md-12 mx-auto">
            <div class="text-center">Customer</div>
            <div
              id="customer-speaking-indicator"
              class="stream-container mx-auto customer-main-tile customer-speaking-indicator"
            >
              <div id="publisher"></div>
              <div
                id="remote-speaking-indicator"
                class="agent-speaking-indicator"
              >
                <div id="subscribers" class="agent-audio-placeholder"></div>
              </div>
            </div>
          </div>
        </div>
      </div>
      <div class="vonage-logo">
        <img
          src="<%= appURI.replace(/\/+$/, '') %>/img/vonage-white.png"
          alt=""
          srcset=""
        />
      </div>
    </div>

    <script
      type="module"
      src="<%= appURI.replace(/\/+$/, '') %>/js/vonage-room/room.js"
    ></script>
  </body>
</html>
